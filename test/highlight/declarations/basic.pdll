#include "ops.td"
// <- keyword.import
//       ^ string

Constraint HasFoo(op: Op);
// <- keyword
//         ^ type.definition
//                ^ variable.parameter
//                    ^ type.builtin

Rewrite Build(root: Op) {
// <- keyword
//      ^ function
  erase root;
//^ keyword
}
